# Post-Commit Evidence Boundary

The final commit SHA, parent, push output, live remote SHA, and clean-tree result
are produced after this single commit and returned in the implementation result.
Embedding them in the same commit would change its SHA.
